// Declaração de Biblioteca padrao de entrada e saida de dados
// std (standard) i (input) o (output) 
#include <stdio.h>  
// Inicio do Programa.           
int main(){
// Comando para exibir o texto hello word na tela
//o comando printf so executa porque voce fez a importação da biblioteca stdio 
 printf("Hello Word");

    return 0;
}